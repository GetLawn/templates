x-lawn:
  compatibility:
    minLawnVersion: 0.2.0
  config:
    POSTGRES_PASSWORD:
      source: generatedSecret
      length: 32
    ADMIN_PASSWORD:
      source: generatedSecret
      length: 32
    DEFAULT_PHONE_REGION:
      value: US
      weight: 10
      options:
        US:
          compose:
            services:
              nextcloud:
                environment:
                  NEXTCLOUD_DEFAULT_PHONE_REGION: US
        GB:
          compose:
            services:
              nextcloud:
                environment:
                  NEXTCLOUD_DEFAULT_PHONE_REGION: GB
        DE:
          compose:
            services:
              nextcloud:
                environment:
                  NEXTCLOUD_DEFAULT_PHONE_REGION: DE
        FR:
          compose:
            services:
              nextcloud:
                environment:
                  NEXTCLOUD_DEFAULT_PHONE_REGION: FR
        NL:
          compose:
            services:
              nextcloud:
                environment:
                  NEXTCLOUD_DEFAULT_PHONE_REGION: NL
        ES:
          compose:
            services:
              nextcloud:
                environment:
                  NEXTCLOUD_DEFAULT_PHONE_REGION: ES
        IT:
          compose:
            services:
              nextcloud:
                environment:
                  NEXTCLOUD_DEFAULT_PHONE_REGION: IT
        PT:
          compose:
            services:
              nextcloud:
                environment:
                  NEXTCLOUD_DEFAULT_PHONE_REGION: PT
        BE:
          compose:
            services:
              nextcloud:
                environment:
                  NEXTCLOUD_DEFAULT_PHONE_REGION: BE
        AT:
          compose:
            services:
              nextcloud:
                environment:
                  NEXTCLOUD_DEFAULT_PHONE_REGION: AT
        CH:
          compose:
            services:
              nextcloud:
                environment:
                  NEXTCLOUD_DEFAULT_PHONE_REGION: CH
        SE:
          compose:
            services:
              nextcloud:
                environment:
                  NEXTCLOUD_DEFAULT_PHONE_REGION: SE
        NO:
          compose:
            services:
              nextcloud:
                environment:
                  NEXTCLOUD_DEFAULT_PHONE_REGION: NO
        DK:
          compose:
            services:
              nextcloud:
                environment:
                  NEXTCLOUD_DEFAULT_PHONE_REGION: DK
        PL:
          compose:
            services:
              nextcloud:
                environment:
                  NEXTCLOUD_DEFAULT_PHONE_REGION: PL
        AU:
          compose:
            services:
              nextcloud:
                environment:
                  NEXTCLOUD_DEFAULT_PHONE_REGION: AU
        CA:
          compose:
            services:
              nextcloud:
                environment:
                  NEXTCLOUD_DEFAULT_PHONE_REGION: CA
        BR:
          compose:
            services:
              nextcloud:
                environment:
                  NEXTCLOUD_DEFAULT_PHONE_REGION: BR
        IN:
          compose:
            services:
              nextcloud:
                environment:
                  NEXTCLOUD_DEFAULT_PHONE_REGION: IN
        JP:
          compose:
            services:
              nextcloud:
                environment:
                  NEXTCLOUD_DEFAULT_PHONE_REGION: JP
        KR:
          compose:
            services:
              nextcloud:
                environment:
                  NEXTCLOUD_DEFAULT_PHONE_REGION: KR
        CN:
          compose:
            services:
              nextcloud:
                environment:
                  NEXTCLOUD_DEFAULT_PHONE_REGION: CN
        RU:
          compose:
            services:
              nextcloud:
                environment:
                  NEXTCLOUD_DEFAULT_PHONE_REGION: RU
        TR:
          compose:
            services:
              nextcloud:
                environment:
                  NEXTCLOUD_DEFAULT_PHONE_REGION: TR
        MX:
          compose:
            services:
              nextcloud:
                environment:
                  NEXTCLOUD_DEFAULT_PHONE_REGION: MX

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: nextcloud
      POSTGRES_USER: nextcloud
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}"
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - "./postgres:/var/lib/postgresql/data"
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "nextcloud"]
      interval: 10s
      timeout: 5s
      retries: 5
    x-lawn:
      environment:

  redis:
    image: redis:7
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  nextcloud:
    image: nextcloud:32
    depends_on:
      - postgres
      - redis
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_DB: nextcloud
      POSTGRES_USER: nextcloud
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}"
      NEXTCLOUD_ADMIN_USER: admin
      NEXTCLOUD_ADMIN_PASSWORD: "${ADMIN_PASSWORD:?ADMIN_PASSWORD is required}"
      NEXTCLOUD_TRUSTED_DOMAINS: "localhost${LAWN_DOMAIN:+ ${LAWN_DOMAIN}}"
      REDIS_HOST: redis
      TZ: "${TIMEZONE:-UTC}"
    volumes:
      - "./www:/var/www"
    ports:
      - "8080:80"
    healthcheck:
      test: ["CMD", "curl", "-f", "--max-time", "5", "http://localhost/status.php"]
      interval: 30s
      timeout: 30s
      retries: 10
      start_period: 180s
    deploy:
      resources:
        limits:
          memory: 1024M
    x-lawn:
      notices:
        - id: login-credentials
          style: info
          elements:
            - id: username
              type: field
              value: admin
            - id: password
              type: field
              source: NEXTCLOUD_ADMIN_PASSWORD
              container: nextcloud
      createDirectories:
        - "./www/html"
      initFiles:
        - source: "z-lawn-override.config.php"
          containerPath: "/var/www/html/config/z-lawn-override.config.php"
      environment:
        POSTGRES_HOST:
        POSTGRES_DB:
        POSTGRES_USER:
        NEXTCLOUD_ADMIN_USER:
        NEXTCLOUD_ADMIN_PASSWORD:
        NEXTCLOUD_TRUSTED_DOMAINS:
        REDIS_HOST:
        TZ:
