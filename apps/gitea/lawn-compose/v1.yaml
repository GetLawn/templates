x-lawn-compatibility:
  minLawnVersion: 0.2.0

x-lawn:
  variables:
    SECRET_KEY:
      source: generatedSecret
      length: 50
      displayName: "Secret Key"
      description: "Secret key for encryption. Auto-generated during install."

services:
  gitea:
    image: gitea/gitea:1-rootless
    environment:
      GITEA__database__DB_TYPE: "sqlite3"
      GITEA__server__HTTP_PORT: "3000"
      GITEA__server__DOMAIN: "${LAWN_DOMAIN:-localhost}"
      GITEA__server__ROOT_URL: "${LAWN_URL:-http://localhost:${HOST_PORT_3000:-3000}}/"
      GITEA__security__INSTALL_LOCK: "true"
      GITEA__security__SECRET_KEY: "${SECRET_KEY:?SECRET_KEY is required}"
      GITEA__service__DISABLE_REGISTRATION: "false"
      GITEA__service__REQUIRE_SIGNIN_VIEW: "false"
      GITEA__openid__ENABLE_OPENID_SIGNIN: "false"
      GITEA__openid__ENABLE_OPENID_SIGNUP: "false"
    volumes:
      - "./data:/var/lib/gitea"
      - "./config:/etc/gitea"
    ports:
      - "3000:3000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/healthz"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512M
    x-lawn:
      notices:
        - id: first-time-setup
          title: First-Time Setup
          style: info
          elements:
            - type: text
              content: "On first visit, you'll see a registration page. The first registered user automatically becomes the administrator."
      healthChecks:
        - type: http
          description: Web UI is accessible
          path: /api/healthz
          expectedStatus: 200
      environment:
        GITEA__database__DB_TYPE:
          description: "Database engine. Set to sqlite3 for zero-configuration setup."
        GITEA__server__HTTP_PORT:
          description: "HTTP port for the web interface."
        GITEA__server__DOMAIN:
          description: "Domain name for the Gitea server. Auto-configured from mDNS hostname when available."
        GITEA__server__ROOT_URL:
          description: "Full public URL of the Gitea server. Auto-configured from mDNS hostname when available."
        GITEA__security__INSTALL_LOCK:
          description: "Skip the installation wizard. Pre-configured for immediate use."
        GITEA__security__SECRET_KEY:
          description: "Secret key for encryption. Auto-generated during install."
        GITEA__service__DISABLE_REGISTRATION:
          description: "Whether to disable user self-registration."
        GITEA__service__REQUIRE_SIGNIN_VIEW:
          description: "Whether to require sign-in to view any page."
        GITEA__openid__ENABLE_OPENID_SIGNIN:
          description: "Enable OpenID sign-in."
        GITEA__openid__ENABLE_OPENID_SIGNUP:
          description: "Enable OpenID sign-up."
